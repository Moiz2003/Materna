# Materna — pinned dependency lockfile
#
# Generated 2026-08-07 by resolving requirements.txt in a clean
# Python 3.12 virtualenv. requirements.txt declares >= floors only,
# which means two installs can resolve to different versions and no
# audit result stays valid. This file pins the exact resolved set,
# including transitive dependencies.
#
# Verified with pip-audit on 2026-08-07: no known vulnerabilities.
#
# Install with:  pip install -r requirements.lock.txt
# Regenerate:    python3.12 -m venv .venv && .venv/bin/pip install -r requirements.txt && .venv/bin/pip freeze > requirements.lock.txt
#
# Note: the Dockerfile targets Python 3.11; this was resolved on
# 3.12. Re-resolve on 3.11 if any pin proves incompatible.

annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
band-client-rest==0.0.10
band-sdk==1.6.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.4.9
click==8.4.2
cryptography==50.0.0
fastapi==0.141.1
filelock==3.32.2
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
iniconfig==2.3.0
packaging==26.3
phoenix-channels-python-client==0.2.3
pillow==12.3.0
pluggy==1.6.0
pycparser==3.0
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic_core==2.46.4
Pygments==2.20.0
pytest==9.1.1
pytest-asyncio==1.4.0
python-dotenv==1.2.2
python-multipart==0.0.32
PyYAML==6.0.3
reportlab==5.0.0
starlette==1.4.1
typing-inspection==0.4.2
typing_extensions==4.16.0
uvicorn==0.52.1
websockets==17.0.1
