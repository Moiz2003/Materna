"""packet — sealed PDF review packet generator (ReportLab)."""

async def build_packet(case: dict, findings: list, flags: list, decision: dict, final_hash: str) -> str:
    """Stub — implemented in P12."""
    raise NotImplementedError("Packet generator — implemented in P12")
